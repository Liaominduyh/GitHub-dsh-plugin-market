# dsw-workshop-plugin 激活补丁：经 dsh plugin add 安装后，
# 本行会自动加入 profile 的 dsh.profile.bundles，启动时作为 bundle 补丁层激活。
# 手动接入其它 profile 时，可把本行复制进该 profile 的 cordis.patch.yml。
- insert:
    - id: dsw-workshop
      name: 'dsw-workshop-plugin'
